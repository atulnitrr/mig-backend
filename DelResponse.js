class DelResponse {
  constructor(pnr, airlinecode, status, remark) {
    this.pnr = pnr;
    this.airlinecode = airlinecode;
    this.status = status;
    this.remark = remark;
  }
}

module.exports = DelResponse;
